
// Game UI by Cody - Edited by Hafsah

import javax.swing.*;
import java.util.ArrayList;

public class GameUI extends JFrame {

    private JTextField ItemName;
    private JTextField ItemDescription;
    private JTextField IsCollectible;
    private JButton btnNext;
    private JButton btnPrevious;
    private JButton btnQuit;
    private JButton btnUpdate;
    private JButton btnDelete;
    private JButton btnAdd;

    private void createUIComponents() {
        // TODO: place custom component creation code here
    }

    // Added By Hafsah - methods for the controller
    
    public void setRoomData(Room room) {
        System.out.println("setRoomData called for " + room.getName());
    }

    public void setInventoryDropdown(ArrayList<Item> items) {
        System.out.println("setInventoryDropdown called, size: " + items.size());
    }
    public void showMessage(String message) {
        JOptionPane.showMessageDialog(this, message);
    }
    public String promptUser(String message) {
        return JOptionPane.showInputDialog(this, message);
    }
}

